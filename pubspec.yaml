name: ddaom_frontend
description: "따옴 - 핫플 포토 가이드 앱"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  camera: ^0.11.0
  gal: ^2.3.0
  google_mlkit_commons: ^0.11.0
  google_mlkit_pose_detection: ^0.14.1

  provider: ^6.1.2
  dio: ^5.7.0
  google_sign_in: ^6.2.1
  flutter_secure_storage: ^9.2.2
  flutter_naver_map: ^1.3.1
  image_picker: ^1.1.2
  permission_handler: ^11.3.1
  geolocator: ^13.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
